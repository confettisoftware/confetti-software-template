[build]
  publish = "."
  command = "echo 'No build required'"
  functions = "netlify"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
