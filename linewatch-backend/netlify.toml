[build]
  publish = "."
  command = "echo 'No build required'"
  functions = "linewatch-backend/netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
